import 'package:flutter/material.dart';

class KualitasAirPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Table(
      children: [

      ],
      border: TableBorder(

      ),
    );
  }

  final List<TableRow> _header = [
    TableRow(

    )
  ];
}